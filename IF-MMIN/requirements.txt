# ── Core training dependencies ────────────────────────────────────────────────
# Install with:  pip install -r requirements.txt
#
# PyTorch must be installed separately with the right CUDA version for your GPU.
# On UCloud check which CUDA version is available with:  nvidia-smi
# Then get the correct pip command from https://pytorch.org/get-started/locally/
# Example for CUDA 11.8:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# Example for CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

numpy>=1.21
scikit-learn>=1.0
h5py>=3.0
matplotlib>=3.4

# ── Notebook / dataset exploration dependencies ───────────────────────────────
# Only needed if you run notebooks/01_dataset_exploration.ipynb
pandas>=1.3
seaborn>=0.11
jupyter>=1.0
